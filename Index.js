// Simple JavaScript example
function greet(name) {
    return `Hello, ${name}!`;
}

console.log(greet("World"));

// Simple counter
let count = 0;

function increment() {
    count++;
    console.log(`Count: ${count}`);
}

increment();
increment();
increment();